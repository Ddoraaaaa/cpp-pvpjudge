#include <iostream>
#include <string>

using namespace std;

int main() {
    // Read data from judge via stdin
    // string data;
    // getline(cin, data);

    // cout << "Received data from judge: " << data << endl;

    // Process the data
    string response = "Hello, judge!";

    // Write the response back to program1 via stdout
    cout << response << endl;

    return 0;
}
